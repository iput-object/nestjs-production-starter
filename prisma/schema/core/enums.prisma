// ============================================================
// core/enums.prisma
// Closed domain enums only — extensible vocabularies live in code
// as String columns + TypeScript constants.
// ============================================================

enum Role {
  ADMIN
  USER
}

enum UserStatus {
  ACTIVE
  BANNED
  RESTRICTED
  SUSPENDED
}

enum IdentifierType {
  EMAIL
  PHONE
}
